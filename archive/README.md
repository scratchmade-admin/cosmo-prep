# Archive — Melissa Cosmetology Prep

Nothing here is live. Kept (not deleted) for history. The live site is `../site/`; current docs
and pending work are listed in `../README.md`.

## old-html/ — superseded landing pages
- `index.html`, `index-v2.html`, `index-v3.html` — the iteration trail of the homepage. All
  replaced by the final `../site/index.html`.

## old-copy/ — superseded copy drafts
- `site-copy.md`, `site-copy-v2.md`, `site-copy-v3.md` — homepage copy iterations; the chosen copy
  is baked into `../site/index.html`.

## old-quiz/ — quiz backups
- `quiz-bank.md` — early outline/schema (superseded by `../quiz-bank-100-draft.md` + the live JSON).
- `quiz-data-rotation-draft.json` — pre-renumber backup of the live quiz data (IDs started at 1).
- `quiz-data-draft.json` — earlier, smaller quiz bank that once sat in `site/`; the live site loads
  only `quiz-data-rotation.json`.

## reference/ — stale or historical
- `README.md` — the old, outdated file index (replaced by the new `../README.md`).
- `curriculum.md` — one-line-per-module summary; the detailed version is `../study-modules-draft.md`.
- `california-cosmetology-study-pack-email.txt` — early handoff/brief email.
- `california-cosmetology-prep-site.zip` — packaged snapshot of `../site/` (regenerable).
